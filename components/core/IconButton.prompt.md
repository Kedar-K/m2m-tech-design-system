Square icon-only button for toolbars and dense controls. Always pass `label` for accessibility.

```jsx
<IconButton label="Settings"><GearIcon/></IconButton>
<IconButton variant="solid" label="Add"><PlusIcon/></IconButton>
```
