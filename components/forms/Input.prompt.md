Text input with label, hint and error slots baked in.

```jsx
<Input label="Work email" type="email" required placeholder="you@company.com" />
<Input label="Site ID" error="Already registered" />
```
